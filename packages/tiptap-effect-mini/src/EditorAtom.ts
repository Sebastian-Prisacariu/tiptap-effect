/**
 * @category atoms
 */
export {
  canRun,
  editor,
  events,
  html,
  instance,
  isActive,
  isEditable,
  isFocused,
  isMounted,
  json,
  mounted,
  mountElement,
  options,
  slice,
  snapshot,
  text,
} from "./internal/editorAtom"

/**
 * @category mutations
 */
export {
  refresh,
  run,
  runSync,
  setContent,
  setEditable,
  setOptions,
} from "./internal/editorAtom"

